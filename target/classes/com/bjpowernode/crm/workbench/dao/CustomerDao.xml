<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bjpowernode.crm.workbench.dao.CustomerDao">
    <!--
    sql语句增删改查操作有对应的标签
    <select>查询操作
    <insert>增加操作
    <update>更新操作
    <delete>删除操作

  
    -->
    <!--根据我们company查询我们Customer表中是否有数据-->
    <select id="findByCompany" resultType="Customer">
        select * from tbl_customer where name = #{company}
    </select>

    <!--对我们company表中的数据进行我们的增加操作-->
    <insert id="saveCustomer">
        insert into tbl_customer
        (
        id,
        owner,
        name,
        website,
        phone,
        createBy,
        createTime,
        contactSummary,
        nextContactTime,
        description,
        address
        )
        values
        (
        #{id},
        #{owner},
        #{name},
        #{website},
        #{phone},
        #{createBy},
        #{createTime},
        #{contactSummary},
        #{nextContactTime},
        #{description},
        #{address}
        )
    </insert>
</mapper>